# Prefer pnpm but don't enforce strictly (for CI/CD compatibility)
engine-strict=false

# pnpm configuration
shamefully-hoist=true
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=@types/*

save-exact=false
